import { PreventKeyseDirectiveDirective } from './prevent-keyse-directive.directive';

describe('PreventKeyseDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventKeyseDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
